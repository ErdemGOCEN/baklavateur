# v3.0.0-qa4 — Mobile product cards

- Products grid uses two columns at widths up to 760px.
- Product image, body padding, headings, descriptions, prices and action buttons are compacted.
- Product descriptions and names are line-clamped to preserve equal card heights.
- Extra compact spacing is applied at widths up to 360px.
- Product images remain centered with `object-fit: contain`.

Build note: sitemap generation completed (192 URLs). The Vite binary was unavailable in the current environment, so a full production bundle was not generated here.
